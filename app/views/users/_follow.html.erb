<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= button_tag(:class => "btn-flat white nomargin nopadding") do %>
      <i class="fa fa-heart-o tooltipped"
       aria-hidden="true"
       data-position="right" 
       data-tooltip="Follow"
       data-delay="20"></i>
  <% end %>
<% end %>